import MobileScren from "../../components/mobile-screen";

function Main (){
    const content =
        <div>content</div>

    return(
        <MobileScren content={content} />
    );
}

export default Main;